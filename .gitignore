# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
build/
dist/
.next/
out/

# Environment variables — NEVER commit .env files with real secrets
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# Secrets, certificates and key material
*.key
*.pem
*.p12
*.pfx
*.crt
secrets/
secret/
stress_test_config.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# Docker
.docker

# OS files
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Project specific
admin-ui/node_modules/
admin-ui/.next/

# Backups e artefatos de banco
backups/
backups_test/
*.sql.gz

# Artefatos de auditoria e relatórios gerados
audit-log-*.csv
relatorio-*.pdf
compliance_report.pdf
TECHNICAL_REPORT.pdf
*.zip

# Artefatos temporários de desenvolvimento
codigo_completo.txt
validador_ui.js
migration_error.log

# Backups de arquivos binários compilados
*.bak
*.wasm.bak
migrations/
admin-ui/test-results/

# OpenClaude runner — third-party fork with GovAI patches.
# Tracked because the patches (Dockerfile.govai, locked-mode bootstrap,
# bundle stub, mkdir of cwd) are required for the runner to start.
# Excluded subdirectories: build artifacts and the runner's own deps.
openclaude/node_modules/
openclaude/dist/
openclaude/build/
openclaude/.cache/
